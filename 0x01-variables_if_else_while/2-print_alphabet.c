#include <stdio.h>
/**
 * main - entry point
 * char c
 *
 * program to check
 * section header
 * Return: always 0
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; ++c)
	{
	putchar(c);
	}
	putchar('\n');
	return (0);
}
