#include <stdio.h>
/**
 * main - entry point
 * char c
 *
 * program to print
 * section header
 * Return: always 0
 */

int main(void)
{
int num;
for (num = 0; c <= 9; ++c)
{
putchar((num % 10) + '0');
}
putchar('\n');
return (0);
}
